# 🚀 Інструкція з оптимізації ZALIZO.STUDIO

## ✅ Виконані оптимізації

### 1. **Preconnect для CDN** ✓
- Додано `preconnect` для cdnjs.cloudflare.com та cdn.jsdelivr.net
- **Результат**: Браузер встановлює з'єднання раніше (-750ms)

### 2. **Async завантаження Font Awesome** ✓
- CSS Font Awesome тепер завантажується асинхронно
- **Результат**: Не блокує відображення контенту (-1,060ms)

### 3. **Async Email.js** ✓
- Скрипт завантажується після основного контенту
- **Результат**: Не блокує парсинг HTML (-780ms)

### 4. **Оптимізація відео** ✓
- Додано `preload="metadata"` замість `auto`
- Додано SVG poster для миттєвого відображення
- **Результат**: Економія ~7 MB трафіку при першому завантаженні

---

## ⚠️ КРИТИЧНО: Оптимізації, які потрібно виконати ВРУЧНУ

### 🔴 1. Оптимізувати SVG логотип (НАЙВАЖЛИВІШЕ!)

**Проблема**: `Дизайн без назви.svg` = **1.8 MB** (неприпустимо для іконки!)

**Рішення А - Використати SVGO (рекомендовано):**

```bash
# Встановити SVGO
npm install -g svgo

# Оптимізувати SVG (зменшить до ~50-200 KB)
cd /workspaces/zalizo/Logo/
svgo "Дизайн без назви.svg" -o "logo-optimized.svg"

# Перевірити результат
ls -lh logo-optimized.svg

# Замінити в index.html
# Змінити: background: url('Logo/Дизайн без назви.svg')
# На:      background: url('Logo/logo-optimized.svg')
```

**Рішення Б - Конвертувати в PNG (якщо SVGO не допоможе):**

```bash
# Використати ImageMagick або онлайн інструмент
convert "Дизайн без назви.svg" -resize 200x200 -background none logo.png
optipng -o7 logo.png  # Стиснути PNG

# Або використати WebP (найкраще стиснення)
cwebp -q 90 logo.png -o logo.webp
```

**Очікуваний результат**: Розмір з 1.8 MB → 20-100 KB (-1,700 KB!)

---

### 🔴 2. Оптимізувати відео файли

**Проблема**: 
- `Video_with_Metal_Gates.mp4` = 3.5 MB
- `From KlickPin...mp4` = 3.7 MB
- **Разом: 7.2 MB** завантажуються при кожному візиті!

**Рішення - Конвертувати через FFmpeg:**

```bash
cd /workspaces/zalizo/Video/

# Оптимізувати desktop відео (ціль: ~1.5 MB)
ffmpeg -i "Video_with_Metal_Gates.mp4" \
  -vcodec libx264 -crf 28 -preset slow \
  -vf "scale=1920:-2" \
  -an \
  "Video_with_Metal_Gates_optimized.mp4"

# Оптимізувати mobile відео (ціль: ~1 MB)
ffmpeg -i "From KlickPin CF Pin by LyfairsAvery on Home Tour [Video] in 2025 _ Design your dream house Big houses interior House design.mp4" \
  -vcodec libx264 -crf 30 -preset slow \
  -vf "scale=1080:-2" \
  -an \
  "mobile_video_optimized.mp4"

# Перевірити розміри
ls -lh *optimized*

# Оновити шляхи в index.html
```

**Параметри пояснення:**
- `-crf 28-30` = якість (нижче = менший розмір)
- `-preset slow` = краще стиснення
- `-an` = видалити аудіо (непотрібно для фонового відео)
- `scale=1920:-2` = масштабувати до потрібного розміру

**Очікуваний результат**: 7.2 MB → 2-2.5 MB (-4.5 MB!)

---

### 🟡 3. Оптимізувати зображення

**Проблема**: `Ворота.webp` = 508 KB (можна зменшити на 53 KB)

**Рішення:**

```bash
cd /workspaces/zalizo/Photos/

# Перестиснути WebP з нижчою якістю
for file in *.webp; do
    cwebp -q 82 "$file" -o "optimized_$file"
done

# Або використати squoosh-cli
npm install -g @squoosh/cli
squoosh-cli --webp '{"quality":80}' *.webp
```

**Очікуваний результат**: -50-100 KB на кожне зображення

---

## 📊 Очікувані результати ПІСЛЯ всіх оптимізацій:

| Метрика | Зараз | Після | Покращення |
|---------|-------|-------|------------|
| **FCP** | 3.9s | ~1.5s | **-2.4s** |
| **LCP** | 3.9s | ~2.0s | **-1.9s** |
| **Розмір сторінки** | 10 MB | ~3 MB | **-70%** |
| **Performance Score** | 79 | 95+ | **+16** |

---

## 🎯 Пріоритети виконання:

1. **🔴 КРИТИЧНО** - Оптимізувати SVG логотип (1 хвилина)
2. **🔴 КРИТИЧНО** - Стиснути відео (5-10 хвилин)
3. **🟡 ВАЖЛИВО** - Оптимізувати зображення (2-3 хвилини)
4. **🟢 ДОДАТКОВО** - Налаштувати CDN з кешуванням

---

## 🛠️ Швидкий старт (все за 5 хвилин):

```bash
# 1. Встановити інструменти
npm install -g svgo @squoosh/cli

# 2. Оптимізувати SVG
cd Logo && svgo "Дизайн без назви.svg" -o logo-opt.svg

# 3. Оптимізувати зображення
cd ../Photos && squoosh-cli --webp '{"quality":82}' *.webp

# 4. Оновити шляхи в index.html вручну
```

---

## 📈 Додаткові рекомендації:

### Кешування (додати в .htaccess або Nginx):
```apache
# Кешувати статичні ресурси на 1 рік
<filesMatch "\.(jpg|jpeg|png|webp|gif|svg|ico|woff2|woff|mp4)$">
    Header set Cache-Control "max-age=31536000, public"
</filesMatch>

# Кешувати CSS/JS на 1 місяць
<filesMatch "\.(css|js)$">
    Header set Cache-Control "max-age=2592000, public"
</filesMatch>
```

### Стиснення Gzip/Brotli:
```nginx
# Nginx конфіг
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
gzip_min_length 1000;
```

### Lazy Loading для зображень у секціях:
```html
<img src="image.webp" loading="lazy" decoding="async" alt="...">
```

---

## ✅ Перевірка після оптимізації:

1. Запустити Lighthouse ще раз
2. Перевірити розміри файлів: `du -sh Logo/ Photos/ Video/`
3. Очікуваний Performance Score: **95+**

---

**Питання?** Запитайте GitHub Copilot! 🤖
