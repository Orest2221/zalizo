# ⚡ Швидкий старт - Оптимізація ZALIZO.STUDIO

## 🎯 Що вже зроблено автоматично:

✅ **Preconnect для CDN** - браузер підключається раніше  
✅ **Async Font Awesome** - не блокує рендеринг  
✅ **Async Email.js** - завантажується після основного контенту  
✅ **Оптимізація відео** - `preload="metadata"` + SVG poster  

**Результат**: FCP покращиться на ~1.5 секунди

---

## 🔥 Що потрібно зробити ВРУЧНУ (5-10 хвилин):

### Варіант А: Автоматичний скрипт (рекомендовано)

```bash
# Запустити автоматичну оптимізацію
./optimize.sh

# Після завершення замінити файли:
mv Logo/logo-optimized.svg "Logo/Дизайн без назви.svg"
mv Photos/optimized/*.webp Photos/
mv Video/optimized/* Video/
```

### Варіант Б: Ручна оптимізація

#### 1. Оптимізувати SVG логотип (КРИТИЧНО!)

```bash
npm install -g svgo
svgo "Logo/Дизайн без назви.svg" -o "Logo/logo-opt.svg"
# Результат: 1.8 MB → 50-200 KB
```

#### 2. Стиснути відео (КРИТИЧНО!)

```bash
# Desktop відео
ffmpeg -i "Video/Video_with_Metal_Gates.mp4" \
  -vcodec libx264 -crf 28 -preset slow \
  -vf "scale=1920:-2" -an \
  "Video/desktop-opt.mp4"

# Mobile відео  
ffmpeg -i "Video/From KlickPin...mp4" \
  -vcodec libx264 -crf 30 -preset slow \
  -vf "scale=1080:-2" -an \
  "Video/mobile-opt.mp4"

# Результат: 7.2 MB → 2.5 MB
```

#### 3. Оптимізувати зображення

```bash
npm install -g @squoosh/cli
squoosh-cli --webp '{"quality":82}' Photos/*.webp
# Результат: -50-100 KB на зображення
```

---

## 📊 Очікувані результати:

| Метрика | До | Після | Покращення |
|---------|-----|-------|------------|
| FCP | 3.9s | ~1.5s | **-61%** |
| LCP | 3.9s | ~2.0s | **-49%** |
| Performance | 79 | 95+ | **+16** |
| Розмір | 10 MB | ~3 MB | **-70%** |

---

## 🚀 Швидке тестування:

```bash
# Перевірити розміри файлів
du -sh Logo/ Photos/ Video/

# Запустити локальний сервер
python3 -m http.server 8000

# Відкрити Lighthouse в Chrome DevTools
# F12 → Lighthouse → Analyze page load
```

---

## 📁 Створені файли:

- `OPTIMIZATION_GUIDE.md` - детальна інструкція
- `optimize.sh` - автоматичний скрипт
- `.htaccess` - кешування для Apache
- `nginx.conf` - кешування для Nginx

---

## 💡 Поради:

1. **Завжди** робіть резервні копії перед заміною файлів
2. Тестуйте на локальному сервері перед завантаженням
3. Після оптимізації перевірте, що всі ресурси завантажуються
4. Запустіть Lighthouse тричі і візьміть середній результат

---

## ❓ Проблеми?

- **SVG не оптимізується**: Конвертуйте в PNG/WebP
- **Відео погана якість**: Збільште CRF до 25-26
- **Помилки ffmpeg**: Перевірте версію `ffmpeg -version`

---

**Успіхів! 🎉**
